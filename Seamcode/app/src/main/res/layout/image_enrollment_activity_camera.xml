<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_print_black"
    tools:context="com.seamfix.qrcode.enrollment.EnrollmentCameraActivity">

    <com.otaliastudios.cameraview.CameraView
        android:id="@+id/camera"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:keepScreenOn="true"
        app:cameraPlaySounds="true"/>

    <ImageView
        android:id="@+id/image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <ImageButton
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:id="@+id/enrollment_camera_capture_button"
        android:layout_marginBottom="40dp"
        android:background="@drawable/circular_button"
        android:src="@drawable/ic_photo_camera_black_24dp"
        android:layout_gravity="bottom|center">

    </ImageButton>

    <LinearLayout
        android:id="@+id/id_frame_processor_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="gone"
        android:layout_gravity="bottom|center">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textStyle="normal"
            android:layout_gravity="center"
            android:background="#77000000"
            android:textColor="#FFFFFF"
            android:padding="10dp"
            android:layout_marginBottom="20dp"
            android:text="@string/text_please_stay_infocus"/>

        <ProgressBar
            android:id="@+id/id_progress_bar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:indeterminate="false"
            android:maxHeight="5dip"
            android:minHeight="5dip"
            android:progress="70"
            android:layout_marginStart="20dp"
            android:layout_marginEnd="20dp"
            android:layout_marginBottom="50dp"
            android:layout_gravity="bottom|center"
            android:progressDrawable="@drawable/drawable_demo_flat_progress_bar" />

    </LinearLayout>



</FrameLayout>